# Cluster -> one provider-terraform Workspace that runs the landing-zone
# fleet/cluster-stack entrypoint (network + cluster wired tofu-natively), then
# maps the entrypoint's outputs back to the Cluster's status.
#
# provider-terraform runs `tofu`, not `terragrunt`, so the Workspace points at the
# plain-tofu entrypoint — not a component directory. The entrypoint owns the AWS
# provider (region + default_tags + an optional cross-account assume_role), so a
# single ProviderConfig (InjectedIdentity = the hub's IRSA) is enough here; the
# per-cluster account targeting rides on the assume_role_arn var below.
apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: cluster-aws
  labels:
    provider: aws
    substrate: terragrunt
spec:
  compositeTypeRef:
    apiVersion: fleet.nanohype.dev/v1alpha1
    kind: XCluster
  writeConnectionSecretsToNamespace: crossplane-system
  mode: Pipeline
  pipeline:
    - step: render-workspace
      functionRef:
        name: function-patch-and-transform
      input:
        apiVersion: pt.fn.crossplane.io/v1beta1
        kind: Resources
        resources:
          - name: cluster-stack
            base:
              apiVersion: tf.upbound.io/v1beta1
              kind: Workspace
              spec:
                providerConfigRef:
                  name: default
                forProvider:
                  source: Remote
                  # module is patched from spec.moduleSource (the cluster-stack entrypoint)
                  enableTerraformCLILogging: true
                  # Index-ordered vars matching fleet/cluster-stack/variables.tf.
                  # A later move to function-go-templating drops the index coupling.
                  vars:
                    - key: region           # 0
                    - key: environment      # 1
                    - key: team             # 2
                    - key: cluster_name     # 3
                    - key: cluster_version  # 4
                    - key: vpc_cidr         # 5
                    - key: assume_role_arn  # 6
                  backendConfig:
                    - key: bucket
                      value: nanohype-eks-fleet-tfstate
                    - key: key
                      value: ""  # patched: fleet/<name>/terraform.tfstate
                    - key: region
                      value: ""  # patched from spec.region
                    - key: encrypt
                      value: "true"
                  writeConnectionSecretToRef:
                    namespace: crossplane-system
                    name: ""  # patched: <name>-kubeconfig
            patches:
              - type: FromCompositeFieldPath
                fromFieldPath: metadata.name
                toFieldPath: metadata.annotations[crossplane.io/external-name]
              - type: FromCompositeFieldPath
                fromFieldPath: spec.moduleSource
                toFieldPath: spec.forProvider.module
              # spec -> tofu vars (fleet/cluster-stack/variables.tf)
              - type: FromCompositeFieldPath
                fromFieldPath: spec.region
                toFieldPath: spec.forProvider.vars[0].value
              - type: FromCompositeFieldPath
                fromFieldPath: spec.environment
                toFieldPath: spec.forProvider.vars[1].value
              - type: FromCompositeFieldPath
                fromFieldPath: spec.team
                toFieldPath: spec.forProvider.vars[2].value
              - type: FromCompositeFieldPath
                fromFieldPath: spec.clusterName
                toFieldPath: spec.forProvider.vars[3].value
              - type: FromCompositeFieldPath
                fromFieldPath: spec.clusterVersion
                toFieldPath: spec.forProvider.vars[4].value
              - type: FromCompositeFieldPath
                fromFieldPath: spec.network.vpcCidr
                toFieldPath: spec.forProvider.vars[5].value
              # spec.vendRoleArn -> the entrypoint's provider assume_role (cross-
              # account). Empty (default) = no assume_role; the entrypoint uses the
              # hub's own credentials, which is the same-account / kind-hub path.
              - type: FromCompositeFieldPath
                fromFieldPath: spec.vendRoleArn
                toFieldPath: spec.forProvider.vars[6].value
              # state key + kubeconfig secret name, per cluster
              - type: FromCompositeFieldPath
                fromFieldPath: metadata.name
                toFieldPath: spec.forProvider.backendConfig[1].value
                transforms:
                  - type: string
                    string:
                      type: Format
                      fmt: "fleet/%s/terraform.tfstate"
              - type: FromCompositeFieldPath
                fromFieldPath: spec.region
                toFieldPath: spec.forProvider.backendConfig[2].value
              - type: FromCompositeFieldPath
                fromFieldPath: metadata.name
                toFieldPath: spec.forProvider.writeConnectionSecretToRef.name
                transforms:
                  - type: string
                    string:
                      type: Format
                      fmt: "%s-kubeconfig"
              # entrypoint outputs -> Cluster status
              - type: ToCompositeFieldPath
                fromFieldPath: status.atProvider.outputs.cluster_name
                toFieldPath: status.clusterName
              - type: ToCompositeFieldPath
                fromFieldPath: status.atProvider.outputs.cluster_endpoint
                toFieldPath: status.clusterEndpoint
              - type: ToCompositeFieldPath
                fromFieldPath: status.atProvider.outputs.cluster_certificate_authority_data
                toFieldPath: status.certificateAuthorityData
              - type: ToCompositeFieldPath
                fromFieldPath: status.atProvider.outputs.cluster_security_group_id
                toFieldPath: status.clusterSecurityGroupId
              - type: ToCompositeFieldPath
                fromFieldPath: status.atProvider.outputs.oidc_provider_arn
                toFieldPath: status.oidcProviderArn
              - type: ToCompositeFieldPath
                fromFieldPath: status.atProvider.outputs.oidc_issuer
                toFieldPath: status.oidcIssuer
              - type: ToCompositeFieldPath
                fromFieldPath: status.atProvider.outputs.karpenter_iam_role_arn
                toFieldPath: status.karpenterIamRoleArn
              - type: ToCompositeFieldPath
                fromFieldPath: status.atProvider.outputs.vpc_id
                toFieldPath: status.vpcId

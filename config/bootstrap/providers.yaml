# provider-opentofu + its runtime tuning. Apply to the hub cluster after
# Crossplane v2 itself is installed (see README.md).
#
# APPLIED BY HAND, NOT BY GITOPS — and it cannot be applied verbatim. The
# ServiceAccount annotation below carries a <FLEET_ACCOUNT_ID> placeholder that the
# hub's account id has to be substituted into at apply time; docs/stand-up-the-hub.md
# does it with a sed as the file is piped to kubectl. No ApplicationSet in eks-gitops
# syncs this directory (checked: nothing under its applicationsets/ references
# eks-fleet's config/, provider-opentofu, or the crossplane-system bootstrap), so
# there is no kustomize replacement filling the placeholder either. If this file is
# ever put under ArgoCD, the placeholder must become a real value or a documented
# kustomize patch first — synced as-is, the SA gets a literal invalid role ARN and
# every vend fails IRSA with no obvious cause.
apiVersion: pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-opentofu
spec:
  package: xpkg.upbound.io/upbound/provider-opentofu:v1.1.4
  runtimeConfigRef:
    name: provider-opentofu
---
# The runtime config carries three things that matter for EKS:
#   1. The IRSA ServiceAccount — the hub's identity, annotated with the fleet
#      account role it assumes (and chains from, into each workload account). The
#      eks-fleet-crossplane role's trust policy names this SA subject
#      (system:serviceaccount:crossplane-system:provider-opentofu).
#   2. --timeout=60m — an EKS build outruns the 20m default and would otherwise be
#      killed mid-apply, leaking a half-built cluster + a stuck lock.
#   3. --poll=1m — provider-opentofu defaults to a 10m drift/output poll; 1m keeps the
#      Cluster status fresh as the Workspace converges.
apiVersion: pkg.crossplane.io/v1beta1
kind: DeploymentRuntimeConfig
metadata:
  name: provider-opentofu
spec:
  serviceAccountTemplate:
    metadata:
      name: provider-opentofu
      annotations:
        # set by the bootstrap to the fleet-account Crossplane role
        eks.amazonaws.com/role-arn: arn:aws:iam::<FLEET_ACCOUNT_ID>:role/eks-fleet-crossplane
  deploymentTemplate:
    spec:
      selector: {}
      template:
        metadata:
          # Scraped by the hub's Grafana Agent → Amazon Managed Prometheus. The
          # provider serves controller-runtime metrics (reconcile rate/errors/
          # latency for the Workspace controller — the vend execution signal) on
          # :8080 by default. Feeds the fleet-vend dashboard in eks-gitops.
          annotations:
            prometheus.io/scrape: "true"
            prometheus.io/port: "8080"
            prometheus.io/path: "/metrics"
        spec:
          containers:
            - name: package-runtime
              # The spoke cluster-bootstrap Workspace installs Cilium + ArgoCD via
              # the helm provider, which derives its repository cache + config from
              # HOME. The provider image leaves HOME unset (= /), where the cache
              # dir is never created and chart resolution fails with "Error locating
              # chart ... index.yaml: no such file". Point HOME at a writable dir
              # so helm can fetch + cache repo indexes.
              env:
                - name: HOME
                  value: /tmp
              args:
                - --debug
                - --timeout=60m
                - --poll=1m
                - --max-reconcile-rate=5

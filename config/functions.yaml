# The composition pipeline function. Installed on the management cluster and
# passed to `crossplane render` for offline validation.
apiVersion: pkg.crossplane.io/v1
kind: Function
metadata:
  name: function-patch-and-transform
  annotations:
    render.crossplane.io/runtime-development-container-image: xpkg.upbound.io/crossplane-contrib/function-patch-and-transform:v0.8.2
spec:
  package: xpkg.upbound.io/crossplane-contrib/function-patch-and-transform:v0.8.2
